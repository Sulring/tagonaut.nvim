local actions = require("tagonaut.floating.actions")

return {
  display_tags = actions.display_tags,
  close = actions.close,
  select = actions.select,
  delete = actions.delete,
  clear = actions.clear,
  assign_shortcut = actions.assign_shortcut,
  rename_tag = actions.rename_tag,
  list_tags = actions.list_tags,
}
